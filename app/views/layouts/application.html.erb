<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "foundation-rails" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
  </head>

  <body>
    <header>
      <div class="logo"><img src="http://i.imgur.com/SePCCpY.png?1"/><p>Find Your Java Fix!</p></div><div class="background"></div>
    </header>

    <div id="search-form"></div>

    <div class="show-results"></div>

    <%= yield %>
    <%= javascript_include_tag "application" %>


    <script type="text/x-handlebars-template" id="cafe-template">
      <div class="cafe" style="background-image:url({{image_url}})">
        <h3>{{name}}</h3>
        <p>{{address}}</p>
      </div>

    </script>

    <script type="text/x-handlebars-template" id="search-form-template">
      <div class='search-form-div'>
        <input type="text" name="address" placeholder="Enter your address here">
        <span class='new-search'>New Search</span>
      </div>
    </script>
  </body>
</html>